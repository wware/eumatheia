exhibit: fastapi-crud
title: "Build a FastAPI CRUD App"
setup:
  - manifest: terminal-pod.yaml
steps:
  - id: intro
    narrative: fastapi-intro.md
    panes:
      - type: terminal
        label: "Shell"
    verify:
      type: manual
    next: run-app

  - id: run-app
    narrative: fastapi-run.md
    panes:
      - type: terminal
        label: "Shell"
      - type: iframe
        label: "App"
        path: "http://localhost:9000"
    verify:
      type: manual
    next: api-docs

  - id: api-docs
    narrative: fastapi-api.md
    panes:
      - type: terminal
        label: "Shell"
      - type: iframe
        label: "App"
        path: "http://localhost:9000"
    verify:
      type: manual
    next: null
